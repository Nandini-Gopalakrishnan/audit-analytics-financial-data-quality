This folder contains the Python workflow used to clean, validate, and analyse the synthetic financial transaction data. It identifies data-quality issues, duplicate invoices, missing approvals, invalid dates, high-value transactions, and unusual transaction amounts using Pandas, NumPy, and IQR-based anomaly detection. The processed outputs are used for the subsequent SQL audit tests and Power BI dashboard.
